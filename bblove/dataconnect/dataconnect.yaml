specVersion: "v1"
serviceId: "bblove"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bblove-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
